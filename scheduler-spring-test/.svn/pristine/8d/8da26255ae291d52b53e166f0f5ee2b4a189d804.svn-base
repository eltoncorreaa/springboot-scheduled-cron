package com.elton.app.service;

import java.util.List;

import com.elton.app.dto.CompanyDTO;

public interface CompanyService {

	/**
	 * Persiste uma instancia da entidade Empresa.
	 *
	 * @param company
	 *        Instancia de Acao a ser persistida
	 * @return entidade persistida
	 */
	CompanyDTO persist(CompanyDTO company);

	CompanyDTO get(Long id);

	CompanyDTO merge(CompanyDTO company);

	void delete(Long id);

	List<CompanyDTO> findAll();

	List<CompanyDTO> buscarPorFiltro(CompanyDTO company);
}
